# materials.js 分割リファクタリング worklog

## 2026-07-30

- task-001: リファクタ前の MATERIALS レジストリを scratchpad/baseline.json にダンプ（45素材・公開エクスポート43件）。結果: 成功
- task-002: 分割スクリプト（行範囲抽出 + import/export 自動付与）で src/materials/ 以下11ファイルを生成。
  - 不具合1: コメント内の識別子言及（例: meta.js のコメント内 updateSeed）を「利用」と誤検出 → コメント除去後の本文で判定するよう修正
  - 不具合2: 元ファイルが CRLF のため行末 \r がコメント除去の正規表現（`.*$`）を阻んでいた → 読み込み時に LF 正規化。retry: 2/3
  - 備考: electricity.js ⇄ registry.js は循環 import（updateLightning 等が実行時に MATERIALS の色テーブルを参照するため）。関数呼び出し時には全モジュール評価済みなので安全。元の単一ファイルでも MATERIALS はファイル末尾定義で同じ意味論だった。
- task-003: src/materials.js を再エクスポートのみのファサード（17行）に置換。内部専用の KOI_BODY / VIBRATION は非公開のまま維持。結果: 成功
- task-004: リファクタ後のダンプと baseline.json を diff → 完全一致（名前・色・update関数ソース文字列まで）。結果: 成功
- task-005: python http.server + Chrome で index.html をロード。ページ由来のコンソールエラー0件（拡張機能由来の3件のみ）、砂の配置・落下を2フレームのスクリーンショット差分で確認。結果: 成功
- task-006: コミットはユーザ承認待ち

=== COMPLETED at 2026-07-30 (task-006 except) ===

## 2026-07-30 第2弾（4段階開発）

- 手順1 熊手（砂紋ツール）: input.js にツールモード・rakeStroke 実装、main.js にパレット登録（キー v）。
  物理不変・色のみ書き換え・冪等（固定パレット塗り）。
  - 検証: Node 機能テスト PASS（707セル再着色、砂のみ、cells/meta 完全不変、歯の行位置一致）
  - 検証: Node で盤面を PNG 描画し目視 — 直線砂紋と岩を囲む同心円の砂紋を確認
  - 障害: ブラウザタブが hidden だと rAF 停止でゲームループが凍る（既存仕様）。
    ブラウザキャッシュで旧コードが動き続ける点にも注意（fetch cache:reload で解決）
- 手順2 四季システム設計: docs/seasons-design.md 作成（seasonGate 方式・影響範囲表・3段階実装順序）
- 手順3 散 + 水琴窟: src/ritual.js（風の掃き消し、3乗カーブ）、src/audio.js（雫・砂擦れ・風・おりん、
  純関数コア + ZenAudio ラッパ）、main.js/index.html 配線（🔔音トグル・🍃散ボタン）
  - 検証: 散 Node テスト PASS（3195→3119→2781→1902→0、600フレームで完全な空）
  - 検証: OfflineAudioContext 実レンダリング計測 — 全4音コントラクト合格
    （初回計測で砂擦れのヒス過多と音量バランスを検出 → LPF追加・ゲイン調整で修復）
  - 未検証: 実際の聴感（ユーザの耳が最終ゲート）
- 手順4 追加ブレスト（規模・時間レンズ）: 最終報告に記載

=== COMPLETED at 2026-07-30 ===

## 2026-08-07 第2弾のコミット + キーボード操作の設計

### 保留されていた3コミットを分割して確定

`src/main.js` に熊手と散の変更が混在していたため、中間状態を作って分割した。
（`git add -p` は当環境で対話不可のため、full 版を退避 → 熊手分だけ残した版でコミット →
full 版を書き戻して2つ目をコミット、という手順を採った）

- `9ba078d` feat: 熊手（砂紋ツール）を追加 — input.js / main.js
- `9833514` feat: 散の儀式と水琴窟の生成音響を追加 — ritual.js / audio.js / main.js / index.html
- `9c4f563` docs: 四季システムの設計書を追加 — docs/seasons-design.md

- 各コミット前に `node --check`（.mjs にコピーして実行）で構文検証。全て PASS
- 不具合: 1つ目のコミットで PowerShell の here-string 記法 `@'...'@` を Bash ツールに
  渡してしまい、メッセージ先頭と末尾に `@` が混入。メッセージをファイルに書いて
  `git commit --amend -F` で修復。retry: 1/3
  → **教訓: Bash ツールでは heredoc、PowerShell ツールでは here-string。混同しない**
- `plan.md` / `worklog.md` は未追跡のまま保留（コミットか .gitignore かは未決）

### 未検証のまま残っているもの

- **音の聴感**: OfflineAudioContext の測定は全項目合格しているが、実際に耳で聴く確認がまだ。
  `python -m http.server 8080` を起動済み。🔔 を押して判定してもらう段階で止まっている

### 下部バー再編 + キーボード操作の設計（docs/keyboard-mode-design.md）

サークルの出し物で自作キーボードから遊べるようにしたい、という要望から。
バーが読めない問題（32素材を1行に並べて約2,500px 必要）と、キーボードで遊べない問題
（1素材1キーで32キー消費・カーソル概念が無い）は、どちらも「グループ×素材の2次元に
組み替える」ことで同時に解けると整理した。

ユーザに確認して確定した3点:
- バーは「グループタブ + 素材行 + 操作行」の3行構造（展開パネル案・常時グリッド案は不採用）
- 描画は決定キーを押している間だけ（トグルは初見が混乱するため不採用）
- 自作キーボードのキー数は未定 → 6 / 10 / 18 キーの3段構成を用意して、
  どう作られても対応できる形にした

調査で判明したこと:
- 素材数は 32（以前の説明で 34 と述べたのは誤り）。README 更新時はこちらが正
- 既存のグループ分けが実態と食い違っている。`'燃焼'` に砂・水・雪・壁、`'岩石'` に酸植物。
  再編して 基本/火/生命/気液/岩石/電気/侘寂/道具 の8グループ・最大6個にする
- `scenarios.js` に `group` / `PALETTE` の参照は0件（grep 確認済み）。
  グループ再編でシナリオは壊れない
- ブラシの `<input type="range">` はキーボードだと「左右で値変更」となり、
  他行の「左右で項目移動」と衝突する → ⊖⊕ の2ボタンに置換する判断

実装は4段階（PR1 バー再編 → PR2 仮想カーソル+描画 → PR3 メニューナビ → PR4 出し物調整）。
未着手。次は PR1 から。

=== 設計完了・実装未着手 at 2026-08-07 ===

## 2026-08-11 第3弾: シナリオ2本増設（金継ぎ + 桜と蛍）

ブレスト（レンズ4本）→ ユーザ選択（①金継ぎ + ⑤桜と蛍）→ 実装 → 検証の一連。

### 実装内容

- 新素材: GOLD=45（金、最重量級の粘性液体）/ KINTSUGI=46（金継ぎ、非パレット・反応生成のみ）
  - src/materials/kintsugi.js 新規（updateGold / updateKintsugi）
  - 化学: 鉱物固体2面以上に挟まれた金だけが凝固（平床では固まらない = 割れ目だけを継ぐ）/
    氷・雪で急冷凝固（蒸気を出す、溶岩→黒曜石の作法）/ 溶岩で金継ぎ→金に再溶解 /
    酸は updateAcid のリスト外なので自動的に不活性（金の骨格が残る）/
    KINTSUGI は CONDUCTOR_IDS[7]（3bitマスク最後の枠）で振動波を伝え、通過後に完全復元+金の火花
- 新イベント: kintsugi_formed（凝固時）/ sakura_bloomed（開花境界）/ firefly_born（蛍自然発生）
- scenarios.js: loadKintsugi（割れた石の器+台座+砂庭、割れ目3本を角度65/105/150°で刻む）/
  loadSakuraFirefly（石張りの池+鯉+ほとりの草花+岩組）。SCENARIOS 先頭に ★→★★ 順で追加
- main.js: PALETTE に 金 [n]（電気グループ。侘寂は6個満杯のため）→ 計33素材
- docs/keyboard-mode-design.md: 電気グループ 2→3、計32→33 に追記

### 検証（Node 直接 + PNG + ブラウザ）

- 一時 package.json {"type":"module"} を置いて Node で実エンジンを回した（検証後に削除済み）
- テストスイート 20/20 PASS（scratchpad/test-kintsugi.mjs）:
  レジストリ整合・割れ目凝固+イベント・平床では液体のまま・酸中で金継ぎ不変+石だけ溶解・
  溶岩再溶解・氷急冷・振動波の通過と11セル完全復元・金継ぎシナリオ実走（注ぐ→固まる→水を湛える）・
  桜シナリオ実走（種→sakura_bloomed→firefly_born 発火）
  - 不具合1（テスト側）: 溶岩が金継ぎの上から横に滑り落ちて接触が切れる（既存の溶岩挙動）
    → 壁の筒で閉じ込めて再実行。retry: 1/3
- PNG 目視: 器の割れ→金の継ぎ目の上に水が湛えられる画を確認。桜は終盤に画面いっぱいの桜吹雪
  （既存の桜素材の挙動。実ウィンドウでは盤面が広いので比率は穏やか）
- ブラウザ実機: カード4枚・素材ボタン33個・金[n]あり・ページ由来のコンソールエラー0件
  （拡張機能由来3件のみ、前回と同一）
- node --check 全10ファイル OK

### 保留

- コミットはユーザ承認待ち
- README 乖離がさらに拡大（シナリオ4本・33素材）。PR1 後の書き直し対象

=== 実装・検証完了 at 2026-08-11 ===

## 2026-08-11 第4弾: 実験帳モード（化学反応の練習）

シナリオとは別枠の練習モード。お題の反応を起こして見せると⭕成功→次のお題、全10題で皆伝。

### 実装内容

- src/practice.js 新規（DOM非依存の PracticeMode + DRILLS 10題）
  - 成功判定は「生成物セル数のベースラインからの増分」— 素材コードは一切変更しない汎用方式
  - 10題: 蒸気/燃焼/石化/ガラス/黒曜石/玄武岩/発芽/墨/感電/金継ぎ（各題に受け皿・砂山・
    石板などの小舞台を自動設置）
  - 成功で ⭕表示 + audio.bell()（ready ガード内蔵なので安全）→ 130フレーム後に次へ
- main.js: 🧪ボタン（開始/実験中はスキップ）、クリアで退出、loadScenario と相互排他、
  ループに practice.step()。ヒントはシナリオバーを共用
- index.html: #practice-btn 追加。docs/keyboard-mode-design.md の操作行3箇所に🧪追記

### 検証

- Node 機械プレイ: 全10題完走 7/7 PASS（scratchpad/test-practice.mjs）。
  各題の所要 131-247 フレーム（正しい手で2〜4秒 — 良いペース配分）
  - 不具合1（テスト側）: ボットが同一セルに置き続けると ①蒸気: 生成物を自分の火で上書き
    ②黒曜石: 融氷→水→溶岩表面に石の蓋ができ氷が届かなくなる → x をずらして
    池の内部に塗る「ブラシ模倣」方式に修正。retry: 1/3。
    ※これは実プレイヤーがブラシで自然にやる動きで、ゲーム側の問題ではない
- ブラウザ実機: 🧪開始→「実験 1/10 蒸気」表示、スキップ→2/10、クリア退出でバー消灯、
  ページ由来エラー0件
- node --check: practice.js / main.js OK。一時 package.json は削除済み

### 保留

- コミットはユーザ承認待ち

=== 実験帳 実装・検証完了 at 2026-08-11 ===

## 2026-08-11 実験帳の情景化 + 化学反応の見直し（ユーザフィードバック対応）

「受け皿ばかりでなくフィールドを活かした様子で」「分かりやすい化学反応に」の2点。

### 舞台の全面改修（受け皿 → 情景）

- 野火（草原に火を放つ・旧: 油の池）/ 湯けむり（岩間の泉）/ 溶岩流（赤土の山肌+溶岩源泉+
  最初から流れる溶岩の舌）/ 雷とガラス（三連の砂丘）/ 火口（石の山体+カルデラ湖39セル）/
  泥田（畦つき）/ 畑（畝+石張りの水瓶）/ 凍る池（旧: 墨。地味だったので差し替え）/
  雷鳴の池（岩の小島つき）/ 金継ぎ（砂庭+台座+稲妻形の割れ目の石碑）

### 化学反応の見直し（デバッグで根本原因を特定して2箇所修正）

- **黒曜石が世界に定着しない問題**: 溶岩+冷却系が必ず「全部石」に収束していた。原因は
  2つの非対称: ①氷が先に融けて水になり水+溶岩→石の皮が湖面を塞ぐ ②黒曜石は溶岩接触
  3%/frame で再溶融するのに石は溶けない
  - fluids.js updateIce: 氷が融ける瞬間、触れていた溶岩を黒曜石に急冷するよう修正
  - geology.js updateObsidian: 再溶融 3%→0.3%（耐久性のある素材に。遅いリサイクルは維持）
- 玄武岩は溶岩で再溶融する仕様（既存）→ 溶岩を扱う舞台の地形には使えない。
  溶岩流の山肌は固い土、火口の山体は石（どちらも溶岩に完全耐性の静的素材）に変更
- 溶岩無限供給（源泉）の火口は黒曜石が下から再溶融して定着しない → 有限の溶岩湖に変更

### 検証

- Node 機械プレイ全10題完走 6/6 PASS（各題 135-211 フレーム）。金継ぎ回帰 20/20 PASS 維持
- 全10舞台のPNG目視（草原の密度と溶岩の舌を1回改修）
- ブラウザ実機: 開始→「野火」表示、スキップ4回→「火口」、クリア退出、エラー0件

=== 実験帳改修完了・push待ち at 2026-08-11 ===

## 2026-08-11 実験帳の画面全体化 + 固定素材の廃止（ユーザフィードバック第2弾）

「空中の小フィールドは見づらい→画面全体に」「固定素材（氷）を置かせる反応は
配置が難しく、反応してるのに判定されない感になる→避ける」の2点。

### 舞台の画面全体化

- 全10舞台を「画面の底から積み上げ、幅いっぱい」に再設計。画面端が壁の役割を
  するので枠は不要。山や池は W/H 比例でスケール（石碑も H 比例に）
- 発見した不具合: 池系3舞台で石の地面を水面より1行高く張ってしまい「石の蓋」に
  なっていた（雪も雷も水に届かない）→ 地表と水面を同じ高さに修正

### 固定素材の廃止（プレイヤーが置く素材は「落ちる・流れる」ものだけに）

- 火口: 氷[o]→雪[3]に変更。雪の急冷を化学に追加（basic.js updateSnow:
  雪が触れた溶岩は黒曜石に固まり雪は融けて水に。氷と同じ作法）
- 凍る池: 雪[3]のみに。水の凍結を 0.3%→1%/frame にバフ（basic.js updateWater。
  0.3%だと雪が凍る前に融けてしまい「反応が見えない」）

### 深掘りデバッグ: 火口が不安定（成功したり詰まったり）だった根本原因

- 断面観察で特定: 深い縦穴カルデラでは融け水が穴に溜まり、凍結バフで
  **氷の栓**ができて溶岩が密封される（氷114-174個・雪1200個が詰まる断面を確認）
- 対策: 縦穴をやめ「台形の楯状火山 + 山頂に開けた広い溶岩湖」に再設計。
  縁の石を溶岩面と同じ高さにして融け水が山腹へ流れ落ちる（溜まらない）構造に
  → 5試行連続 PASS（103-121f）

### 検証

- Node 機械プレイ 2回連続で全10題完走（火口244-245f・凍結313-407f）
- 金継ぎ回帰 20/20（T5が1回だけ確率揺らぎで落ちたが3連続再実行で全合格）
- 全舞台PNG目視 + ブラウザ実機（開始/スキップ/退出・エラー0件）

=== 画面全体化完了 at 2026-08-11 ===

## 2026-08-11 実験帳の作成要領を文書化 + 検証ハーネスをリポジトリに恒久化

- docs/practice-drill-guide.md 新規: これまでの経緯（v1受け皿→v2情景化→v3画面全体化）、
  お題の選定基準、舞台設計の原則、判定設計、落とし穴カタログ8件（全部実際に踏んだもの）、
  追加手順チェックリスト、文体、今後のお題候補5件
- 検証ハーネスを scratchpad（セッション限り）から tests/ に移植:
  - tests/practice.test.mjs（機械プレイ全10題 + 舞台PNG出力）
  - tests/scenario.test.mjs（金継ぎの化学+シナリオ回帰20項目）
  - PNG は tests/out/ に出力（.gitignore に追記）
- 移植後の実行確認: practice 6/6 PASS・scenario 20/20 PASS
- 不具合: .gitignore を Write で上書きしてしまい既存5行を消失 → git diff で検出し復元。
  教訓: 既存ファイルは必ず読んでから書く。retry: 1/3
- 備考: docs/zen-sand-はじめてガイド.html/.pdf（ユーザ作成？）が未追跡で存在。触っていない

=== 作成要領の文書化完了 at 2026-08-11 ===

## 2026-08-11 実験帳 README 整備

- task-308: 初期6素材だけを説明していた README を、現在の操作・実験帳の10題・33素材・
  検証手順への導線を含む利用案内へ更新。作成要領を唯一の詳細手順書として参照する形にした。
- 検証: `node tests/practice.test.mjs` を2回実行して各回6/6 PASS、
  `node tests/scenario.test.mjs` も20/20 PASS。一時 ESM 設定は検証後に削除。

=== README整備・回帰検証完了 at 2026-08-11 ===

## 2026-08-11 第5弾: 実験帳の反応題増設

- task-401 着手: 既存題と重ならず、動的素材だけをプレイヤーに置かせられる反応を
  素材コードと作成要領から選定。油田爆発／夜光洞／墨田の夜を単体プローブで評価する。
- task-401 結果: 8試行ずつの単体プローブで、油田爆発は炎ピーク 1136〜1211、
  夜光洞は発光菌ピーク 165〜173、墨田の夜は暗黒植物ピーク 133〜353。3候補とも
  8/8で目標を達成したため採用。
- task-402/403: `DRILLS` を10→13題へ拡張し、画面全体の舞台と正答ボットを追加。
  - 油田の雷鳴: 油田へ雷 [t]。油→炎の面変化。
  - 夜光洞: 菌床へ油 [5]。菌→発光菌の青緑変化。
  - 墨田の夜: 炭田へ水 [2]、種 [w]。炭→墨泥→暗黒植物の二段階変化。
  - 成功回数を固定値で持っていたテストを `DRILLS.length` 基準へ変更。
    追加直後の失敗をここで検出・修正。retry: 1/3。
- 舞台画像を確認し、初期景観が低すぎたため油田の貯留層、洞窟の鍾乳石、炭田の土手を追加。
  炭田の土手が反応面を覆って一度 STALL したため、両端へ移設。retry: 1/3。
- task-404: `node tests/practice.test.mjs` を2回実行して各回6/6 PASS（全13題）、
  `node tests/scenario.test.mjs` も20/20 PASS。新舞台PNGを目視確認、
  `node --check src/practice.js` / `src/main.js` OK。
- task-405: この環境には接続可能なブラウザがなく、ブラウザ実機スモークは未実施。
  コード・機械プレイ・画像・回帰検証には影響なし。ブラウザ利用可能時の確認待ち。

=== 第5弾 実装・自動検証完了 at 2026-08-11 ===

## 2026-08-11 第6弾: 手順明確化・手動遷移・新題追加

- task-501 着手: 成功後の自動遷移を廃止し、明示的な「次へ」操作に置換する。
  墨田の夜は二段階の投入手順を画面表示で案内する。
- task-502 着手: 砂+溶岩→砂岩の「砂の炉」を8試行以上で成立確認する。
- task-502 結果: 単体プローブ8/8 PASS（砂岩ピーク36〜51、目標10）。「砂の炉」を追加し、
  `DRILLS` を13→14題へ拡張。
- task-501: 墨田の夜を「①黒い炭田へ水 [2] → ②できた黒い墨泥へ種 [w]」として
  中間生成物（墨泥10セル）で案内を自動更新する二段階題へ変更。成功後は `ready` 状態で
  反応を継続し、🧪ボタンを「次へ ➜」へ切り替える。自動遷移は廃止。
- task-503: `node tests/practice.test.mjs` を2回実行して各回8/8 PASS（全14題）。
  成功後240フレーム待っても自動遷移しないこと、`skip()`（次へ操作）だけで次題を読むことを
  専用テストで確認。`node tests/scenario.test.mjs` も20/20 PASS、
  `node --check src/practice.js` / `src/main.js` OK。砂の炉の舞台PNGを目視確認。
- task-504: 接続可能なブラウザがないため未実施（第5弾と同じ環境制約）。

=== 第6弾 実装・自動検証完了 at 2026-08-11 ===

## 2026-08-11 実験帳 変更メモ

- `docs/practice-mode-changelog.md` を新設。現在の14題、手動遷移、墨田の夜の操作手順、
  追加題、検証済み・未実施の項目を、作業ログとは別に利用者向けに要約した。

## 2026-08-11 PR1: 下部バーの再編

`docs/keyboard-mode-design.md` §1・§2・§7-1 の実装。マウス操作のみ・素材の挙動不変。

- task-101: `PALETTE` を8グループへ再編（基本/火/生命/気液/岩石/電気/侘寂/道具）。
  タブ順は `GROUPS = [...new Set(PALETTE.map(m => m.group))]` で PALETTE 自身から導出するので、
  並びとタブがずれることがない。結果: 成功
  - 検証: 旧版（`git show HEAD:src/main.js`）と新版の PALETTE を正規表現で抽出して比較。
    (id, label, color, key) の集合が完全一致・32件、キー重複なし、
    グループ別件数 4/4/5/4/5/2/6/2 が設計書と一致、各グループが配列内で連続。**15項目 PASS**
- task-102: `index.html` を3行構造（`#group-tabs` / `#mat-row` / `#controls`）へ。
  ブラシスライダーを ⊖ / 数値 / ⊕ に置換。結果: 成功
- task-103: `main.js` にタブ生成・`renderMatRow()`・`showGroup()` を追加、`selectMaterial` を改修。
  結果: 成功
- task-104: `style.css` の `#toolbar`（横スクロール + マスク）を撤去し `.nav-row` の3行構造へ。
  `.icon-btn` → `.ctl-btn` に改名、ドット 8px→12px（+ 暗い素材用に 1px のリング）。結果: 成功
- task-105: ヘッドレス Chrome を CDP で駆動して実機検証。**25項目 PASS**
  - 構造（8タブ・初期選択・ブラシ初期値3）、全8タブで素材行がはみ出さないこと、
    タブ切替は表示のみ・選択据え置き、キー入力で該当タブが自動で開く、
    ブラシ⊖⊕の上下限クランプ、砂を置いて45フレームで落下（最下端 y 163→343）、
    ページ由来のコンソールエラー0件
  - 幅 1920 / 1280 / 680 / 480 / 390 / 360 の6通りで横溢ゼロを実測
- task-106: コミットはユーザ承認待ち

### このPRで決めた設計判断（設計書に記載が無かった点）

**「表示中グループ（タブ）」と「選択中の素材/道具」を別状態にした。**
熊手（道具）を選んだまま「基本」タブを眺める状態があり得るため。タブは表示を変えるだけ、
素材選択は表示グループを自動でそちらへ動かす。これで「キーを押したのに素材行に無い」が起きない。

設計書 §5 の `tabindex="-1"` は PR3 に回した。決定キーの誤発火対策であり、
PR1 の「見た目だけ」の範囲を超えて Tab キーの挙動を変えてしまうため。

### 検証手法のメモ（次回も使う）

- **ブラウザ拡張が未接続でも CDP で直接駆動できる。** Node 22 はグローバル `WebSocket` と
  `fetch` を持つので、依存パッケージゼロで
  `chrome --headless=new --remote-debugging-port=N` → `/json/list` → WS → `Runtime.evaluate`
  でページ内アサーションが書ける。スクリプトは
  `scratchpad/verify-ui.mjs` / `measure-narrow.mjs` / `shot.mjs`
- 障害: ハーネスが `PointerEvent('pointerdown')` を投げていて砂が置けなかった。
  **`input.js` の描画は `mousedown`/`mousemove`/`mouseup` 駆動**で、`pointerdown` は
  `main.js` の音トリガ専用。製品側ではなくテスト側のバグ。retry: 1/3
- 障害: `--force-device-scale-factor=2 --screenshot` の組み合わせだと、撮れた画像が
  メディアクエリ適用後の状態とずれた（390px 幅でバーが切れて見えた）。
  **狭い幅の見た目確認は `Emulation.setDeviceMetricsOverride` + `Page.captureScreenshot` を使う。**
  CDP で実測すると全幅で横溢ゼロだった
- コンソールエラー判定では `Log.entryAdded` の `entry.url` も残すこと。
  favicon の 404 は `entry.text` に "favicon" を含まないので、text だけ見ると誤検出する

=== PR1 COMPLETED at 2026-08-11 ===
（`fd79ccb` 設計書 / `9e7c5af` 実装 + plan.md・worklog.md 追跡開始。master、未 push）

## 2026-08-11 README の全面書き直し

素材表が初期6種のまま、シナリオ・雨・音・散・熊手の記載ゼロという状態を解消した。
PR1 でグループ名が変わったのに合わせての更新でもある。

- 素材表を 8グループ・32種に。3行バーの見かた、操作一覧、反応でのみ生まれる素材14種、
  シナリオ2本、ディレクトリ構成、PR1〜PR4 の進捗チェックリストを追加
- 挙動の説明は**すべて実装を読んで裏を取った**。推測で書かない
- 検証: README の素材表と `PALETTE` を機械照合するスクリプトを書いた
  （32件・キー一致・説明の有無・8見出しの存在・旧記述の残存）。**76項目 PASS**

### 書きながら見つかった自分の誤り（実装を読んで修正）

- **氷と泥を取り違えていた。** `fluids.js:146` の「火→石 / 溶岩→玄武岩」は `updateMud` の中身で、
  `updateIce`（164行〜）ではない。氷は火・溶岩・酸・雷で水に戻り、隣の水を凍らせる側
- 溶岩 + 雪 → **黒曜石** + 蒸気（`basic.js:132`）。水の場合の石とは別物
- 水は雪・氷が隣にあると自分が凍る（`basic.js:54`）。「雪を溶かす」のは火のほう
- 菌は「暗がりで広がる」のではなく、地面を這うように横と下へ広がる（`life.js:361-367`）
- 炭は「ゆっくり燃える」のではなく、火や雷が触れて初めて着火する（`basic.js:78` / `electricity.js:101`）

### 照合スクリプトの罠

旧グループ名「ツール」の残存チェックが「ビルド**ツール**なし」に誤反応した。
語の残存を見る時は `### ツール` / `|ツール|` のように**文脈込みのパターン**にすること。

=== README 更新 COMPLETED at 2026-08-11 ===

## 2026-08-12 第7弾: 実験帳の新題追加（codex-delegate 初のワーカー委任）

第5弾+第6弾を `57b9c71` としてコミット+push（ユーザ承認済み。コミット前に
practice 8/8・scenario 20/20 を再確認）した上で、Codex（gpt-5.6-terra）に
spec.md（作成要領準拠・液体/落下素材限定を絶対条件化・報告様式を本文埋め込み）で委任。

### Codex の成果（検収済み）

- 採用2題: 錆の海（rust-sea、水辺の石浜に金属 [r] → 錆、goal 8）/
  蛍の沢（firefly-brook、草むらの沢に水 [2] → 蛍、goal 8）。DRILLS 14→16題
- 不採用4候補と理由の報告あり: 花粉の風（セル数判定で測れない）・桜前線（120f未満で速すぎ）・
  酸性庭（不安定）・玄武岩の泥化（120f未満）
- 素材コード変更なし。ドキュメント3件（README/作成要領§0/変更メモ）の題数も更新済み
- 報告様式は今回も spec.md 本文埋め込みで初回から遵守（run-codexcomp の知見を再確認）。
  無申告逸脱・余計なファイルなし。tmp-probe/ と一時 package.json の後片付けも遵守

### 元請け検収

- git diff 精読: 対象5ファイルのみ・指示範囲内
- node tests/practice.test.mjs ×2回連続 8/8 PASS（rust-sea 306〜343f・firefly-brook 351〜390f、
  目安 120〜450f 内）、tests/scenario.test.mjs 20/20 PASS
- 舞台PNG目視: 両題とも画面全体・底積み上げで成立
- ヒントのキー表記照合: 金属 [r] は main.js パレットと一致

### 保留

- コミットはユーザ承認待ち
- ブラウザ実機スモーク未実施（第5弾から継続の環境制約。Chrome 接続時に 🧪→16題・新2題の表示確認）

=== 第7弾 実装・検収完了 at 2026-08-12 ===
